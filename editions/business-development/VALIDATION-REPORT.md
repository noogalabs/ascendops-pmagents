# Business Development edition validation

Frozen package date: 2026-08-25. Seat: `business-development`.

## Inputs and seam disposition

- Semantic authority: `mapping-tables/bd.md` and the fictional Ridgeline fixture.
- Source authority: the reviewed 42-question BDM questionnaire and 67-file BD template.
- Declared structured artifact: `business-development-config.json` (AF-1).
- The three proposed comparison rows are absent pending an expressible contract:
  BD-2 compared differently shaped platform prose and returns only after shared
  measure application; BD-1 compared prose ordering operands and returns only
  if the contract mandates numeric or labeled sources; BD-4 compared manager
  identity with approval-threshold policy and is mis-subjected.
- The owner speed-to-lead cover value is a strictly positive integer, lands in
  `config.json` at `/speed_to_lead_minutes`, and renders into the playbook and
  pipeline-board operating surfaces. The reviewed companion prose was scrubbed
  so its old hardcoded five-minute claims defer to that configured authority.
- Day-mode follows the leasing X2 precedent: no pointer or holding row ships
  while the maintenance B8 owner value cannot be expressed without inventing a
  local fallback. Boot surfaces read the structured config paths when the
  maintenance peer registers and otherwise expose `awaiting maintenance peer`.
  PR13b task `task_1787735414502_98348643` owns holderless held-pointer state
  plus pointer-path window extractors;
  the leasing communication-window and BD day-mode rows return together after
  that capability lands.
- Missing maintenance/leasing/accounting/PM-assist owners use the existing peer-absent
  state. Nothing is silently unified and no owner value is invented.
- BD-14 is recorded as a structural lane from Business Development to Leasing.

## Five-law resweep

| Law | BD disposition | Result |
|---|---|---|
| Authority vs fixture | Named people occur only in the fictional Ridgeline fixture; operational surfaces use configured placeholders/answers. | PASS |
| Resolved identity | Manager, broker, counsel, handoff, access, and referral routing resolve from C2-C7. | PASS |
| Fail direction | Empty/unconfirmed routing remains surfaced and unrouted; it never suppresses visibility. | PASS |
| Freshness is not identity | No author predicate is satisfied by timestamp or recency alone. | PASS |
| Reachability disposition | Question-led selling, escalation, and handoff skills are present in the recursive library and reached through their configured roles. | PASS |

## Skill-to-SOUL parity

`SOUL.md` and `question-led-selling/SKILL.md` both bind discovery to questions,
truthful claims, explicit gates, and graceful walk-away. The recursive banned-token
census is zero. No skill weakens the SOUL's fair-housing, contract-change, legal,
fee-deviation, or red-flag-property never-graduates.

## Armed proof

- Edition tests print `ARMED` before declared-filename, create/rerun, setup tree
  equality, label rendering, member census, speed/domain, day-mode absence,
  promise-surface, and honest-empty-check casualties.
- Engine suite: 92 passed.
- BD edition suite: 11 passed; the filesystem-derived CI loop also runs all four
  previously shipped edition suites in separate processes.
- Review sweeps: 6 passed, including the hash-anchored BD promise ledger and
  companion/file parity at first-consumer grain.
- Member hygiene: CLEAN with zero baseline/allowlist rows added.
- Maintenance sealed core SHA-256 remains `0540ea08...3551`.
- Frozen maintenance baselines and two-fresh-run determinism remain green.

## Shared-file touches

`engine/engine.py` (edition constant and one registry row only), `setup.py`,
`README.md`, `PROVENANCE.md`, `engine/edition-review-ledger.json`,
`tests/test_manifest_generation.py`, `provenance/source-files.tsv`, and
`MANIFEST.sha256`. The coordinator must sequence these shared-file changes; this
package integrates no other seat worker.

# Turnover edition validation report

Contract: `pmagents-five-seat-editions-contract-2026-08-25.md`.

## Edition census

- Questionnaire: 34 question IDs, A1-A6, B1-B6, C1-C7, D1-D7, E1-E8.
- Cover: four shared fields plus five turnover setup values.
- Declared artifact: `turnover-config.json`.
- Library source: ten owner-reviewed make-ready documents.
- Shared-file touches are limited to the turnover registry and setup-label rows,
  edition review ledger, provenance and runbook metadata, the edition-suite census,
  and the tracked manifest.

## Five-law resweep

| Law | Result | Running evidence |
|---|---|---|
| No private company/customer identity | PASS | `ci/member-hygiene.py` over every tracked non-self file |
| No private people/contact data | PASS | the same zero-hit census; Ridgeline identities are fictional |
| No credentials or secret material | PASS | Leak Guard plus registry-parameterized production-entry scan casualties |
| No internal evidence paths/codenames | PASS | member hygiene and product-relative provenance |
| No allowlist expansion to excuse a hit | PASS | no hygiene allowlist row changed |

The table cites mechanisms that actually run. `ARMED` text is diagnostic output,
not evidence by itself; each claim below is tied to a production-entry assertion or
an independently run repository gate.

## Mapping and configured artifact

The mapping declares the five turnover cover fields as data. The default turn target
uses `labeled_integer` with the exact `Default turn target days` label; an earlier
unbound number cannot be configured. The target plus inspection SLA, scope SLA, and
stale-stage alert values declare strict-positive domains and reject zero before any
output exists. The remaining turnover answers remain available in the structured
artifact under their declared question IDs.

The custom `turnover-config.json` path passes create-then-reconfigure through the
canonical shared boundary. A maintenance fixture cannot substitute for turnover's
own content-divergent fixture: it rejects on turnover-specific rows before changing
the configured tree.

## Seat library boundary

The first turnover production test exposed that the canonical mapping runtime does
not yet consume a documents-only `SUPPORTED[seat].library`: a valid configuration
initially produced zero of the ten turnover documents because the imported package
contained documents but no bootstrap tree. Ruling
`1787725889788-collie-zc4pl` confirmed that `SUPPORTED[seat].library` is the full
agent-template source, not a second runtime copy source. PR7 therefore imports the
reviewed turnover bootstrap template and carries the ten sanitized companion files at
its top level, matching the leasing precedent. The named production test requires all
ten files in configured output with zero seat-local engine machinery.

## Cross-seat dispositions

Leasing's `move_out_inspection_owner` pointer targets turnover D2 and
`/answers/D2` exactly, matching turnover SEAM-2. Turnover D2 genuinely asks who
conducts the three inspection stages. The old timezone SEAM-19 fact row is absent:
its declared measure is not applied by the current engine and its local `/company`
subject is not part of the structured turnover payload. It can return only after the
shared measure-application capability supplies a normalized, resolvable subject.

The four permanent human gates remain declared in `never_graduate`: make-ready scope
and budget, deposit deductions, vendor pricing, and damage-charge notices. The
questionnaire and fixture state the shipped human-release posture and do not promise
an automated digest, automatic shadow exit, or autonomous message-class graduation.

## Companion and promise truth

The questionnaire and fixture name exactly the ten shipped make-ready documents.
Every imported document footer was swept so it no longer names unshipped checklists,
templates, grading frameworks, or internal-role addenda. The registry-wide companion
sweep binds that claim to the file tree.

Turnover's promise ledger is hash-anchored from birth. Every candidate subject,
including E8, has a review disposition; changing its wording forces fresh review.
The imported bootstrap initially advertised a manual placeholder path and repeated
the configuration interview at first boot. The corrected README names `setup.py` as
the only configuration path and `turnover-config.json` as the structured source of
truth. ONBOARDING now verifies those configured values and collects only deployment
credentials. Its three documented schedules use the leasing-hardened completion
form: unresolved fields stop activation, the cron set is recreated idempotently,
pre-marker failure rolls back role crons, `.onboarded` is the durable boundary, and
heartbeat registration occurs only after that boundary. Named tests pin all five
properties and run `bash -n` over every embedded Bash block.

The shipped onboarding skill now follows the same verify-not-reenter custody as
`ONBOARDING.md`: repository `setup.py` is the single configuration interview,
first boot reads `turnover-config.json`, and only private Telegram deployment wiring
may be collected. A named casualty bans the prior reverse-interview instructions so
the triggerable skill cannot overwrite configuration that the canonical setup path
already rendered.

## Member setup

`python3 setup.py` derives the turnover seat number and all five cover fields from
the registry and mapping. The guided path asks and collects E1-E8 through the shared
question-ID surface. Direct and guided production entries produce byte-equal output,
and the runbook names `turnover-coordinator` plus its declared structured artifact.
